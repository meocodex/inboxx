# ============================================
# CRM WhatsApp Omnichannel - Frontend
# ============================================
# Copie este arquivo para .env e preencha os valores
#
# O frontend roda na porta 5000 (servido pelo Fastify).
# Em desenvolvimento: builde com "npm run build" e copie para api/public/.
# Em producao: o Dockerfile da raiz faz isso automaticamente.

# URL da API (deixe vazio para usar /api relativo - recomendado)
# Em producao com dominio separado, defina o valor completo:
# VITE_API_URL=https://seu-dominio.com/api
VITE_API_URL=

# Sentry (Monitoramento de Erros) - Opcional
# VITE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
